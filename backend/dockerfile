<<<<<<< HEAD
FROM python:3.12-slim

WORKDIR /app 

COPY backend/requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY backend/ .  
COPY .env .

ARG BACKEND_PORT=8000
ENV BACKEND_PORT=${BACKEND_PORT}

CMD uvicorn main:app --reload --host 0.0.0.0 --port ${BACKEND_PORT}
=======
FROM python:3.12.1-bullseye
WORKDIR /app

COPY backend/ /app
COPY .env /app/.env
RUN pip install --no-cache-dir -r requirements.txt

ARG BACKEND_PORT=8000
ENV BACKEND_PORT ${BACKEND_PORT}


CMD ["sh", "-c", "alembic upgrade head && uvicorn main:app --reload --host 0.0.0.0 --port ${BACKEND_PORT}"]
>>>>>>> 87a96d3 (feat: alembic for music tables and extension creation)
